
@page
@using CoogMusic.Pages
@model CoogMusic.Pages.Report.Listener_ReportModel
@{
    ViewData["Title"] = "Report";
}

<div>
    <h1 class="text-center">Listeners Report</h1>
    @* <form method="get">
            <div class="form-group">
                <label for="selectedAlbumId">Select an album:</label>
                <select class="form-control" id="selectedAlbumId" name="selectedAlbumId">
                    @foreach (var album in Model.Albums)
                    {
                    <option value="@album.Id">@album.Title</option>
                    }
                </select>

            </div>
            <button type="submit" asp-page-handler="GenerateListenerReport" class="btn btn-primary">Generate Report</button>
        </form> *@
    <form method="post">
        <select asp-for="SelectedAlbumId" asp-items="@(new SelectList(Model.Albums, "Id", "Title"))">
            <option value="">-- Select an album --</option>
        </select>
        <button type="submit" asp-page-handler="GenerateReport">Generate Report</button>
    </form>


    @if (Model.ReportHtml != null)
    {
    <div class="mt-3">
        <h3 class="text-center mb-3">Listener Report for Album @Model.SelectedAlbumTitle</h3>
        @Html.Raw(Model.ReportHtml)
    </div>
    }
</div>
